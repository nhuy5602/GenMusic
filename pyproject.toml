[project]
name = "genmusic-vn"
version = "0.1.0"
description = "Vietnamese text-to-MP3 local client with Kaggle MusicGen generation."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
kaggle = ["kaggle"]
dev = ["pytest"]

[tool.setuptools.packages.find]
include = ["genmusic_vn*"]
